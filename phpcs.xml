<?xml version="1.0"?>
<ruleset name="Gitwire">
	<description>PHP coding standards for the Gitwire plugin.</description>

	<!-- Scan these paths. -->
	<file>gitwire.php</file>
	<file>uninstall.php</file>
	<file>autoload.php</file>
	<file>includes</file>
	<file>tests</file>

	<arg name="extensions" value="php"/>
	<arg name="colors"/>
	<arg value="sp"/>

	<!-- Use WordPress Coding Standards. -->
	<rule ref="WordPress"/>

	<!-- Allow short array syntax []. -->
	<rule ref="WordPress">
		<exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
	</rule>
	<rule ref="Generic.Arrays.DisallowLongArraySyntax"/>

	<!--
		Tests document themselves through their method names, and fixtures need real
		file operations rather than WP_Filesystem.
	-->
	<rule ref="Squiz.Commenting.FunctionComment">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<rule ref="Squiz.Commenting.VariableComment">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<rule ref="Generic.Commenting.DocComment">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<rule ref="WordPress.WP.AlternativeFunctions">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<rule ref="WordPress.PHP.NoSilencedErrors">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<rule ref="WordPress.PHP.DevelopmentFunctions">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<rule ref="Generic.CodeAnalysis.UnusedFunctionParameter">
		<exclude-pattern>tests/*</exclude-pattern>
	</rule>
	<!-- stubs.php holds WP function stubs, not a class. -->
	<rule ref="WordPress.Files.FileName">
		<exclude-pattern>tests/stubs.php</exclude-pattern>
	</rule>

	<!--
		Flags anything newer than the declared PHP floor. Without the
		PHPCompatibilityWP standard below, testVersion is inert: it is only read by
		that sniff set, so a stray 8.1 feature would have passed review and fataled
		on the 8.0 sites the header promises to support.
	-->
	<rule ref="PHPCompatibilityWP"/>

	<!--
		Both values track the headers in gitwire.php and readme.txt. Change them
		together or this stops checking what the plugin actually claims to support.
	-->
	<config name="minimum_supported_wp_version" value="7.0"/>
	<config name="testVersion" value="8.0-"/>
</ruleset>
